apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
data:
  index.html: |
    <!DOCTYPE html>
    <html>
    <head>
      <title>GitOps Demo</title>
    </head>
    <body>
      <h1>{{ .Values.message }}</h1>
      <p>Aplicacion desplegada con Kubernetes y Helm.</p>
    </body>
    </html>
